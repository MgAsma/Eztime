import { HypenDirective } from './hypen.directive';

describe('HypenDirective', () => {
  it('should create an instance', () => {
    const directive = new HypenDirective();
    expect(directive).toBeTruthy();
  });
});
